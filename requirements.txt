# ── Runtime ───────────────────────────────────────────────────────────────────
pandas>=2.2.0
pyarrow>=15.0.0

# AWS (lazy-imported in code; needed for runtime and integration tests only)
boto3>=1.34.0
botocore>=1.34.0

# Database (Redshift loader)
psycopg2-binary>=2.9.9

# ── Testing ───────────────────────────────────────────────────────────────────
pytest>=8.0.0
pytest-cov>=5.0.0

# ── Code quality (pinned so local and CI always use identical formatters) ──────
black==26.5.1
isort==8.0.1
flake8==7.3.0
